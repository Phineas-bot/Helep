{{- range $name, $svc := .Values.services }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-%s-config" $.Release.Name $name | trunc 63 | trimSuffix "-" }}
data:
{{- if $svc.env }}
{{ toYaml $svc.env | indent 2 }}
{{- else }}
  LOG_LEVEL: "info"
{{- end }}
{{- end }}
