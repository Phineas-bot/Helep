# sos-service subchart values (kept minimal; umbrella values drive actual settings)
replicaCount: 1
service:
  port: 8002
persistence:
  enabled: true
  size: 100Mi
ingress:
  enabled: false
networkPolicy:
  enabled: false
